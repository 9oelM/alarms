import React, { ReactNode } from "react";
import { Normalizer, Padding } from "@/styled/Padding";

export const OUT_MOST_VIEW_PADDING: number = 38;

export const DAYS_PICKER_ITEM_MARGIN_RIGHT: number = 5;

export const normalizer8px: ReactNode = <Normalizer height="8px" />;
export const padding25px: ReactNode = <Padding height="25px" />;
